server:
  port: 8081
spring:
#  application:
#    name: userService
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_user?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
#  cloud:
#    nacos:
#      server-addr: localhost:8899 nacos访问地址
#      discovery#:
#        cluster-name: HZ #配置集群名称
#        ephemeral: false #设置为非临时实例
mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS


#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:8065/eureka